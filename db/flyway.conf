# =============================================================================
# LOCAL DEV ONLY — do not commit production credentials to this file.
# For Cloud SQL or any non-local environment, override via Flyway env vars
# (FLYWAY_URL, FLYWAY_USER, FLYWAY_PASSWORD) or CLI flags (-url, -user,
# -password) at invocation time. Never edit and commit this file with prod
# values.
# =============================================================================
flyway.url=jdbc:postgresql://postgres:5432/jly
flyway.user=jly_admin
flyway.password=localdevpassword
flyway.schemas=core,membership
flyway.defaultSchema=public
flyway.locations=filesystem:/flyway/sql
flyway.baselineOnMigrate=true
flyway.validateOnMigrate=true
